class DDPGModel:
def DDPG(n_states, n_actions, opt, supervised):
    assert isinstance(n_states, object)
    model = DDPGModel(n_states, n_actions, opt, supervised)
    return model

def DDPGModel(n_states, n_actions, opt, supervised):
    assert isinstance(n_states, object)
    model = DDPGModel(n_states, n_actions, opt, supervised)
    return model


# Path: AML/Synthetic/naru/models/__init__.py
class NARUModel:
    # Path: AML/Synthetic/naru/models/__init__.py
    class NARUModel:
    # We use the same model for both the encoder and the decoder.
        # Path: AML/Synthetic/naru/models/__init__.py
def NARUModel(tconfig, params):
    model = NARUModel(tconfig, params)
    return model




def NARU(tconfig, params):
    model = NARUModel(tconfig, params)
    return model

# Path: AML/Synthetic/naru/models/__init__.py
